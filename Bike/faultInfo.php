<?php
include 'connect.php';
        $id
        if( $data = $mysqli->query("SELECT , status FROM awarie") ) {
            
            while ($row = $data->fetch_assoc()) { 
              
            }
            $data->close();
        }
         
?>